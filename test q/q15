def countStringsWithPrefix(words, pref):
    count = 0
    for word in words:
        if word.startswith(pref):
            count += 1
    return count

# Example usage
words = ["pay", "attention", "practice", "attend"]
pref = "at"
print(countStringsWithPrefix(words, pref))  # Output: 2
